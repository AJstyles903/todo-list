{
	"scripts": {
		"dev:client": "cd client && npm run dev",
		"dev:server": "cd backend && npm run dev",
		"dev": "concurrently -n 'BACKEND,FRONTEND' -c 'yellow,blue' \"npm run dev:server\" \"npm run dev:client\""
	},
	"devDependencies": {
		"concurrently": "^9.2.0"
	}
}
